"use client";

/**
 * Re-export cmdk Command primitives.
 * Styled via command-palette.tsx, not here.
 *
 * Note: CommandShortcut is not exported by this version of cmdk — omitted.
 */
export {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
} from "cmdk";
